spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orgflow
    username: orgflow
    password: orgflow

# JWT設定
jwt:
  issuer: orgflow-api
  secret: ${ORGFLOW_JWT_SECRET}
  access-token-expires-in: 1h