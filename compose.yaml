services:
  db:
    image: postgres:17.6
    environment:
      POSTGRES_DB: orgflow
      POSTGRES_USER: orgflow
      POSTGRES_PASSWORD: orgflow
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: