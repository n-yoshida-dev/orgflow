---

## `orgflow/docs/er/00_notes.md`

- なぜ `organization_membership` を、`user` と `organization` の中間概念として独立させるのか
- `request_series_id` と `version_no` を使って、再申請前後の `request` をどう識別するのか
- `approval_policy` と `applied_approval_route` を、どこまで別の概念として分けるのか
